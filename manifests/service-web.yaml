apiVersion: v1
kind: Service
metadata:
  name: fossology-web
  namespace: fossology
spec:
  selector:
    app: fossology-web
  ports:
  - port: 80
    targetPort: 80
    nodePort: 30080
    protocol: TCP
    name: http
  type: NodePort
